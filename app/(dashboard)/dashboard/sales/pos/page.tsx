import { POSSystem } from '@/app/(dashboard)/dashboard/sales/pos/components/pos-system';

export default function POSPage() {
  return (
    <div className="h-screen overflow-hidden">
      <POSSystem />
    </div>
  );
}
